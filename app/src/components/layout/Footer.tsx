export function Footer() {
  return (
    <footer className="border-t border-card-border py-6">
      <div className="mx-auto flex max-w-7xl flex-col items-center justify-between gap-2 px-4 text-xs text-muted sm:flex-row sm:px-6 lg:px-8">
        <p>Halal Protocol — a CPI-indexed stablecoin governed by an on-chain DAO.</p>
        <p>No warranties. Verify contract addresses before signing any transaction.</p>
      </div>
    </footer>
  );
}
